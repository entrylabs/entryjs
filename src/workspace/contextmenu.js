'use strict';

goog.provide('Entry.ContextMenu');

(function(ctx) {
    ctx.X_PADDING = 20;

    ctx.Y_HEIGHT = 20;

    ctx.visible = false;

    this.svgGroup = null;

    this._board = null;


    ctx.createDom = function() {
    };

    ctx.show = function(xy, options) {
    };

    ctx.hide = function() {
    };


})(Entry.ContextMenu);
